a = 10
b = 0

# Формула для оператора assert:
# Если условие True, то выполняется действие ниже, а если условие ложное, то выводится
# сообщение об ошибке

assert b != 0, "На 0 делить нельзя"
print(a / b)